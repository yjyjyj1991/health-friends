<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.healthfriend.healthfriend.model.mapper.UserMapper">

	<insert id="insertUser" parameterType="userDto">
		insert into tbl_user(purpose_id, email, name, nickname, password, isWithdraw,weight,active_point )
		values (#{purpose_id}, #{email}, #{name}, #{nickname} , #{password}, #{isWithdraw},#{weight},#{active_point});
	</insert>

    <update id="updateUser" parameterType="userDto">
        update tbl_user
		set nickname = #{nickname}, password = #{password} , active_point = #{active_point}
		where id = #{id};
    </update>

    <update id="deleteUser" parameterType="userDto">
        update tbl_user
		set isWithdraw = 1, withdraw_reason = #{withdraw_reason}
		where id = #{id};
    </update>
</mapper>